{
  "name": "reaktor-assignment",
  "version": "0.1.0",
  "description": "Root package.json to make it possible to deploy to heroku",
  "scripts": {
    "postinstall": "npm install --prefix api && npm install --prefix client && npm run build --prefix client"
  }
}
